<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doannv.bandthoai">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.DienThoaiActivity" />
        <activity android:name=".activity.LaptopActivity" />
        <activity android:name=".activity.LienHeActivity" />
        <activity android:name=".activity.ThongTinActivity" />
        <activity android:name=".activity.ChiTietSpActivity" />
        <activity android:name=".activity.GioHangActivity" />
        <activity android:name=".activity.ThongTinKhachHangActivity"></activity>
    </application>

</manifest>